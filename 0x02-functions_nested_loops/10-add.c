#include "main.h"
/**
 * add - adds tow integers and returns the result
 * @a: parameter to add
 * @b: parameter to add
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
